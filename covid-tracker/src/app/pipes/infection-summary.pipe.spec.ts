import { InfectionSummaryPipe } from './infection-summary.pipe';

describe('InfectionSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new InfectionSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
