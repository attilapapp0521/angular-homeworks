import { InfectionHighlightDirective } from './infection-highlight.directive';

describe('InfectionHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new InfectionHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
